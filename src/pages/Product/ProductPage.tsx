import React, { FC } from "react";
import { Typography } from "@material-ui/core";

const ProductPage: FC = () => {
  return <Typography variant="h3">Product Page</Typography>;
};

export default ProductPage;
