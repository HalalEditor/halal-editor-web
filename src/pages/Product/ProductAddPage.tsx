import React, { FC } from "react";
import { Typography } from "@material-ui/core";

const ProductAddPage: FC = () => {
  return <Typography variant="h3">Add Product Page</Typography>;
};

export default ProductAddPage;
